# DIALOGUE_RULES.md

この文書は、`Village of Bacchus` の会話・セリフを追加または修正するときの運用ルールです。より細かい実装メモは `docs/dialogue-refactor-handoff.md` にあります。

## 基本方針

- セリフは原則として、対象キャラクター本人の発話にする。
- 状況説明だけで終わらせず、本人がどう感じているかを入れる。
- 口調は肉体ではなく精神側を優先する。
- 肉体状態、通常時の行動、年齢、性別が通常とは異なる組み合わせでも破綻しない文にする。
- 世界観は神話、開拓村、村落生活、祈り、魔素、奇跡、祭、畑、森、酒場など既存語彙に寄せる。
- 現代的なサービス名、端末名、ネット文化、メタ発言は、強い意図がない限り避ける。

## 置き場所

通常会話は、できるだけ `js/data/dialogue/` 配下へ追加します。

- `statusLines.js`: 健康、疲労、消耗、襲撃中など。
- `conditionLines.js`: 負傷、疫病、飢餓、凍え、精神状態など。
- `seasonLines.js`: 春、夏、秋、冬の季節会話。
- `jobLines.js`: preferredAction に応じた会話。
- `reproductionLines.js`: 妊娠、臨月、産褥、成人化など。
- `exchangeLines.js`: 肉体交換への反応。交換の奇跡と落雷による肉体交換で共有する。
- `exchangeSituationLines.js`: 入れ替わった身体の年齢・性別・種族・能力と、交換後半年以内の違和感に応じた通常会話。
- `secretTreasureLines.js`: 秘宝の専用結果モーダルで表示する口調別反応。村人全員が対象になる秘宝（腕の無い天使像など）は代表3人が並ぶため、実口調キーを直接持たせる。
- `miracleLines.js`: 清拭など、分離済みの奇跡結果モーダルで表示する口調別反応。
- `visitorLines.js`: 訪問者タイプ別会話と、来訪時・勧誘や誘惑での加入時のセリフ。黙示録中は同ファイルの黙示録用テーブルへ切り替わる。
- `captiveLines.js`: 収監中の様子、懐柔・誘惑での加入時、解放時、加入後3ヶ月の会話。
- `randomEventLines.js`: ランダムイベント用の口調別セリフ。
- `toneProfiles.js`: 口調キー、alias、fallback、口調解決。

建築要望と個人願望のセリフは、それぞれ `js/data/buildingRequestData.js` と `js/data/wishData.js` にあります。どちらも「理由や望みを述べる中立な本文」と「口調ごとの頼み方」を分けた二層構造で、本文は口調を含めず、建物名や願いの言葉は口調側の枠で足します。要望や願望を増やすときは本文だけを書けば、口調差分は自動で付きます。語り口そのものが芯になるセリフは、要望側の `rawLines` に置くと枠で包まずそのまま表示します。

恋愛、結婚、一部の奇跡結果など、専用モーダル用セリフにはまだ処理ファイル内に残っているものがあります。修正時は表示経路を確認し、既存構造に合わせてください。
肉体交換への反応は `exchangeLines.js` を更新し、交換の奇跡側とランダムイベント側で内容が分岐しないようにしてください。

## 口調キー

保存データにある基本値は `speechType` です。ただし、会話表示では精神特性から仮想口調キーを算出します。

主な口調キー:

- 子供: `赤子`, `男児`, `女児`
- 成人男性: `普通Ｍ`, `丁寧Ｍ`, `強気Ｍ`, `乱暴`, `お調子者`, `陰気`, `クールＭ`
- 成人女性: `普通Ｆ`, `丁寧Ｆ`, `お嬢様`, `快活`, `内気`, `強気Ｆ`, `蓮っ葉`, `おっとり`, `ぶりっこ`, `クールＦ`, `ギャル風`, `中性的`
- その他: `老人`, `狼`, `ゴブリン`

`spiritSex` と口調キーの対応:

| 区分 | 口調キー |
| --- | --- |
| 精神男性側 | `普通Ｍ`, `丁寧Ｍ`, `強気Ｍ`, `乱暴`, `お調子者`, `陰気`, `クールＭ`, `老人` |
| 精神女性側 | `普通Ｆ`, `丁寧Ｆ`, `お嬢様`, `快活`, `内気`, `強気Ｆ`, `蓮っ葉`, `おっとり`, `ぶりっこ`, `クールＦ`, `ギャル風`, `中性的` |
| 仮想子供口調 | `赤子`, `男児`, `女児` |
| 専用口調 | `狼`, `ゴブリン` |

注意:

- `赤子` は精神特性 `無垢` から算出する。
- `赤子` は生後 6 か月程度の自然さを基準にし、意味のある単語や説明的な発話は避ける。喃語、泣き声、唸り声、寝息、必要最小限のしぐさ説明で状況を出す。
- `男児` / `女児` は精神特性 `萌芽` と精神性別から算出する。
- `赤子` / `男児` / `女児` は保存データに書き込まない。
- `中性的` は女性口調側のキーとして扱う。
- `老人` は明確に精神男性側の口調として扱う。
- `狼` は原則として鳴き声や唸り声のみ。必要な場合だけ括弧で簡単な行動や状態を補う。
- `ゴブリン` は粗野な口調。くどくならない範囲で語尾に「ゴブ」を混ぜる。

## 口調解決

通常会話の実効口調は `resolveDialogueTone(character)` で決まります。

1. `mindTraits` に `無垢` があれば `赤子`。
2. `mindTraits` に `萌芽` があれば、`spiritSex` に応じて `男児` または `女児`。
3. それ以外は `speechType` を正規化して使う。
4. `speechType` がなければ、`spiritSex` に応じて `普通Ｍ` または `普通Ｆ`。

ランダムイベントの成人向けセリフでは、保存値としての `speechType` を優先する経路があります。通常会話とランダムイベントでは参照経路が少し違うため、修正時は呼び出し元を確認してください。

襲撃者は生成時点で `js/domain/raiderSpeechTypes.js` のタイプ別口調に固定します。捕虜から加入する時も同じ helper を再適用し、既存セーブや例外経路のずれを防ぎます。

収監中の捕虜だけは例外で、本人の発話にせず、外から見た様子として書きます。捕虜を読めない相手のままにしておくことで、懐柔・誘惑の成功率を台詞から推測できないようにするためです。解放時と加入時からは一人称へ切り替えます。

勧誘・誘惑で訪問者が加入する時のセリフは、通常会話と同じ `getVisitorLineKey()` で引きます。加入処理は名前の「◯◯の」を削るため、キーは改名の前に確定させます。

加入時と解放時のセリフは、ログではなく会話モーダルで一度だけ見せます（`openCharacterLineModal()`）。顔グラと本人の発話だけを残し、会話の引き直しや行動ボタンは出しません。

捕虜まわりのセリフは、引くキーが二種類あります。収監中の描写と加入時のセリフは襲撃者の種類をまとめた属性キー（`無頼`、`規律`、`神聖`、`狼` など）で引き、`getCaptiveGroupKey()` を共有します。解放時と加入後3ヶ月の会話は口調キーで引きます。属性側は、狂信や種族の性質が口調より前に出る場面に使います。

## fallback の使い方

fallback は便利ですが、男女差や当事者性が混線しやすい場所です。

- 妊娠、産褥、通常時の行動、身体変化など、話者によって自然な反応が変わる場面では実口調キーを直接書く。
- 状況キーを定義した通常会話では、原則として実効口調キーごとの明示行を用意し、fallback は例外除けとして扱う。
- `polite`, `bold`, `rough`, `cool`, `bright`, `male`, `female` などの共有 fallback には、誰が読んでも破綻しない文だけを置く。
- 既存データには古い fallback 中心の書き方も残っている。追加・修正時は、可能な範囲で実口調キー方式へ寄せる。

## 複数人が同時に話す場面

ランダムイベントや奇跡の結果モーダルでは、複数の村人の反応を並べて表示します。同じ口調の村人が並ぶとセリフが被るため、次の方針で扱います。

- 村全体へ一律に効くランダムイベント（花火師の来訪、秘湯発見、猛暑、大雪）は、`SINGLE_SPEAKER_EVENTS` に登録して代表1人だけ反応させる。
- 2人が当事者になるランダムイベント（喧嘩、言い争い、痴話喧嘩、雪解け、趣味仲間、男同士の友情、百合の恋）は、口調ごとに2パターンのセリフを配列で持たせる。1つ目を先に口を開く側、2つ目を受け返す側として書き、並べた時に応酬として読めるようにする。
- 複数人が話す場合、`RandomEvents.createEventLine` へ話者の並び順を `variantIndex` として渡す。配列で2つ以上の候補があれば、話者ごとに別の候補が選ばれる。話者が1人の場合は従来どおりランダムに選ぶ。
- 奇跡の結果モーダルは代表3人までを表示し、同じセリフになる村人は選ばない。取れるセリフが少なければ表示人数のほうを減らす。
- 奇跡の結果モーダル用のセリフは、`polite` や `bold` などの共有 fallback ではなく実口調キー20種を直接持たせる。fallback だけに頼ると、丁寧Ｍ・丁寧Ｆ・お嬢様のように別の口調が同じ文へ落ちる。
- 肉体交換モーダル（交換の奇跡、交換の奇跡・強、松かさの杖、牧神の管笛）も同じ画面に複数人が並ぶため、`BODY_EXCHANGE_REACTION_LINES` から引いたセリフを1画面の中で重複させない。牧神の管笛は6人分を先に確定させてから描画する。こちらは表示人数を減らさず、候補を使い切ったときだけ重複を許す。

## セリフ作成のチェックリスト

- 本人の発話になっているか。
- 通常時の行動や状態を、外から説明するだけの文になっていないか。
- 精神年齢・精神性別と口調が合っているか。
- `赤子` が意味語を話していないか。必要なら括弧でしぐさだけを補っているか。
- 肉体交換や特殊な状態でも不自然すぎないか。
- 現代的すぎる単語が混ざっていないか。
- 同じ内容のセリフが別ファイルに重複していないか。
- `default` や fallback に、特定の性別・年齢・肉体状態にしか合わない文を置いていないか。
- 配列が空のとき、会話エンジン側で安全に fallback できるか。
- 複数人が同時に話す場面では、同じ口調の村人が並んでも被らないか。

## 実装時の注意

- 通常会話は `getConversationLine({ character, village })` を入口にする。
- 専用シーンの 1 行取得は `getDialogueLine({ character, scene, key, context })` を使う。
- セリフ選択の優先度は `CONVERSATION_PRIORITY` に従う。
- 入れ替わり後の特殊状況会話は季節・健康・仕事会話と同じ `NORMAL` とし、重傷などの高優先度状態を妨げない。
- `襲撃者` や `訪問者` は口調ではなく状態・役割として扱う。
- `妊娠`, `臨月`, `産褥`, `疫病`, `負傷`, `季節`, `通常時の行動`, `イベント種別` は口調キーにしない。
- 新しい scene を追加する場合は、データファイル、`getDialogueLines`、呼び出し元の表示経路を一緒に確認する。
- セリフ内に実装メモやデバッグ用の注釈を混ぜない。

## レビュー観点

会話変更のレビューでは、文法や誤字だけでなく次を確認します。

- そのセリフを読む人物が本当にその状態の当事者か。
- 口調 fallback で別性別・別年齢の人物に読まれても成立するか。
- 世界観から浮いた語彙がないか。
- 保存済みの `speechType` と仮想口調キーを混同していないか。
- ランダムイベント、通常会話、専用モーダルのどの経路で使われる文か明確か。
